# Changelog

## 0.1.1

- Fix npm `bin` entry so global `xsearchlane` CLI installs correctly

## 0.1.0

- Initial release: MCP server, CLI, SDK client, mock provider
- Direct upstream X search provider via Responses API `x_search`
- Hosted client path for `/v1/xsearchlane/*`
- CLI: `search`, `research`, `health`, `doctor`, `pricing`, `capabilities`, `mcp`
- Python package `talocode-xsearchlane` (SDK + CLI)
- OpenCode MCP example config
- Stacklane patch notes in `docs/STACKLANE_PATCH.md` (deploy separately)
